"use strict";

var BaseCacheEngine = require("./BaseCacheEngine.js");

class PlainLocalStorageDriver extends BaseCacheEngine {
  /**
   * Set the storage engine
   */
  storage = localStorage;
}

module.exports = PlainLocalStorageDriver;
