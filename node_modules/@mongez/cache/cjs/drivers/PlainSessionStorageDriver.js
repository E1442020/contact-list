"use strict";

var BaseCacheEngine = require("./BaseCacheEngine.js");

class PlainSessionStorageDriver extends BaseCacheEngine {
  /**
   * Set the storage engine
   */
  storage = sessionStorage;
}

module.exports = PlainSessionStorageDriver;
